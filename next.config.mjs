/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['res.cloudinary.com','images.unsplash.com', 'assets.aceternity.com'], // Combine all domains here
  },
};

export default nextConfig;
